# Open steals rollup (P0 / P1 only)

Mechanical extract from `entries/*` where `steal_rank` is P0 or P1 and status ≠ done/wont.
Refresh when adding signals: edit this table + entry Status.

> **Reconciled against the live boards 2026-08-08** (audit: every steal checked for a real card
> across all 9 kanban boards, 320 cards). Prior wire statuses were stale in **both** directions —
> five steals were already shipped or carded while still marked `open`, and four had never reached
> a board at all. Wire status below is card-verified, not asserted.

| Steal | From | Rank | T1000-shaped action | Wire status |
|-------|------|------|---------------------|-------------|
| **The first memory lever with NO engine dependency — and the compression is already done for our exact models** | SIG-20260812-04 REAP / llm-compressor v0.13.0 (RedHat_AI) | **P0** | **REAP (Router-weighted Expert Activation Pruning) structurally DELETES low-saliency MoE experts** — `S_j = (1/N_j) Σ g_j(t)·‖f_j(t)‖₂`, router gate × L2 norm of the expert's output over the tokens routed to it. Verified first-party: `vllm-project/llm-compressor` **★3,662 Apache-2.0**, tag `0.13.0` published **2026-08-11 21:08Z** (`prerelease:false`), PR **#2864** merged 06-30 (10 files, +1,823/−0), paper [arXiv 2510.13999](https://arxiv.org/pdf/2510.13999). **The decisive property: the pruned artifact is a normal checkpoint with fewer experts, NOT a new format.** Quantizing to FP8/NVFP4 afterwards is an *optional second step* that locks it to vLLM; stop after the prune and it converts to GGUF like anything else. That makes it the only lever surfaced this week whose payoff needs no engine we lack — vLLM blocked (#51920), SGLang unbenched, DS4 fork unadopted, DSpark NEGATIVE-FINAL. **And we do not even need to run it:** HF `?search=REAP&sort=downloads` returns a live shelf covering our fleet — `mradermacher/OpenAI_GPT-OSS-120B_Pruned_REAP_58B-…-i1-GGUF` (4,144 dl; base `12bitmisfit/…` 200 OK, `config.json` = `num_local_experts 64` / `num_experts_per_tok 4` / 36 layers ⇒ **128→64 experts, 50% prune**; Q4_K_M 44.75 / Q3_K_M **36.31** / Q2_K_S 34.02 GB) and `heath0xFF/DeepSeek-V4-Flash-0731-REAP-K216-GGUF` (**216/256 experts = 15.6% prune**, UD-IQ3_XXS, 3 shards = **82.94 GiB**, MIT, GB10-tested at 32K/64K/**128K**). Also on the shelf: `saricles/MiniMax-M2.5-REAP-139B-A10B-NVFP4-**GB10**` (23,196 dl), `0xTank/Kimi-K3-IQ1S-REAP568-64K-**4XSPARKS**`, first-party `unsloth/GLM-4.7-Flash-REAP-23B-A3B-GGUF` (21,192 dl). **Measured ryan-spark at card time: 121 GB total / 78 used / 43 available**, `gpt-oss:120b` 65 GB + `hermes3:8b-16k` 6.9 GB both `Forever`, disk 3.3 TB free — **Q3_K_M at 36.31 GB fits inside the free 43 GB, so the gpt-oss A/B may run WITHOUT evicting the incumbent** (verify headroom; KV at ctx 131072 is not free). | **CARDED ST-15 `t_ee732ca8`** (steals board; created `ready`, force-blocked, gate = Ryan's word). **Phase 0 = GGUF header range-reads only — free, read-only, no window, releasable alone.** ⚠️ **Run the gpt-oss arm FIRST, not DS4** — the paper's own table shows Qwen3-style MoE recovering **98.86%** (25% prune) and **99.80%** (50%) on GSM8K while the **DeepSeek-V3-arch** model falls to **79.77%** and collapses to **16.99%**; expert redundancy is architecture-specific and DS4 is DeepSeek-family. 🔴 **Corroborated in the field on our exact silicon:** the K160 build (37.5% prune) was **withdrawn by its own maintainer** — `Q2_K` and `Q4_K_M` *"repeatedly exhausted the output budget restating their plans without reaching an answer"* in controlled llama.cpp tests, and full GPU offload of `Q2_K` **and** `MXFP4` *"exposed backend kernel failures on NVIDIA GB10."* K216's gentler 15.6% + **byte-for-byte retained-expert row copying from Unsloth's stock GGUF** (ships `metadata/REAP_K216_PLAN.json` + `tools/reap_k216_gguf.py`) is the safer construction. 🪤 **`twaggs88/DeepSeek-V4-Flash-REAP25-DSpark-ds4-GGUF` is a TRAP — do not pull it.** 12,197 downloads and a name engineered to look like the DSpark GGUF we swept 40 repos for. Four checks fail: its declared base `deepseek-ai/DeepSeek-V4-Flash-DSpark-0731` → **401** against a control pair (real base 200, known-fake 401); its own card admits *"the v4 line is **not** expert-pruned"* and the REAP-25 name is stale branding; it *"runs **only** on the [pulsar] engine… llama.cpp and other GGUF loaders **will not accept**"* its custom formats; `config.json` is `{}`. **SIG-20260811-04 §5 stands — there is still no DSpark drafter GGUF we can run.** ⛔ Do NOT run llm-compressor to make our own checkpoints (calibration needs the full model resident + a dataset, and its natural output target is the blocked engine). ⛔ Do NOT make this a **fourth** claimant on the one consumer-paused ryan-spark window already contested by `t_99c6388b` / `t_6e8222b2` / `t_7cb87a81`. ⛔ Do NOT score any arm against DS4 Flash's live p50 — that lane is still on the `app/` rollback binary (~13.5 t/s vs the fork's banked 25–26); `pct_of_reference` needs a **named** ceiling |
| **Recovery-vs-sparsity is a per-architecture curve — a REAP row without its expert count is unreadable** | SIG-20260812-04 (method) | **P1** | Add `prune_ratio`, `experts_retained/experts_total`, `source_repo`, and `recovery_vs_baseline` on the same prompt set to the bench contract. Same defect class as a spec-dec row without `n_max` or a tok/s row without a named ceiling — the number is uninterpretable without the provenance field. Prune ratios do **not** port between model families (Qwen3 MoE ~99% recovery at 50%; DeepSeek-arch 17%) | **folded into `t_6e058ca2`** (bench provenance), not carded separately. First REAP row written must carry these |
| **Auditable transformation beats download count as a trust signal for third-party weights** | SIG-20260812-04 (supply-chain rule) | **P1** | The **12,197-download** repo is the unusable one (nonexistent base, third-party-engine-only, empty `config.json`); the **386-download** repo ships the plan JSON and the script that produced it, and derives byte-for-byte from a first-party GGUF. Extend the supply-chain rule Ryan set on `t_08e96127` (*"dont install something with only 10 downloads"*) with its complement: **prefer artifacts that ship the transformation that made them.** Applied by construction, this rule catches the twaggs88 trap without needing to read the card | **open — no card** (rule addition; pairs with the existing veto on `t_08e96127`) |
| **The supply-chain objection to vLLM is ANSWERED — and a runtime objection replaces it. Engine phase is a BUILD, not a download** | SIG-20260812-03 vLLM v0.27.0 (mr_r0b0t -> release notes) | **P0** | **Official artifacts, read live:** GitHub release `v0.27.0` published 2026-08-10 21:18Z (`prerelease: false`) and **official PyPI `vllm-0.27.0-cp38-abi3-manylinux_2_28_aarch64.whl`, 307.2 MB** (0.27.1 same, 08-11 11:00Z). That is a first-party wheel from the vLLM project — a different proposition from the 10-download third-party asset Ryan vetoed. **PR #49904 `[Build] Fix CUDA arch detection producing kernel-less builds on SM121`** merged 07-28: on GB10/CUDA 13 the default source build silently emitted **only `sm_75` cubins**, because torch's Fermi-era `string(REPLACE "2.1" "2.1(2.0)")` rewrites **`12.1`** to `12.1(2.0)` -> `-gencode arch=compute_20,code=sm_121`; FP8 then died with `No compiled cutlass_scaled_mm for a compute capability less than CUDA device capability: 121`. **Our box supports the build today:** CUDA **13.0** toolkit at `/usr/local/cuda-13.0` + **gcc 13.3.0** + `CMakeLists.txt:129` CUDA-13 branch lists **`12.1`** in `CUDA_SUPPORTED_ARCHS`. | **Commented on `t_08e96127` — retargeted, NOT released. No new card** (board 57 blocked). **Gate: vLLM issue #51920 closing.** 🪤 **#49904 is a SOURCE-BUILD fix and does NOT apply to the published wheel** — the release build arg is `torch_cuda_arch_list='7.5 8.0 8.6 8.9 9.0 10.0 11.0 12.0'` with *"Do not add +PTX here"*, while every CUTLASS/FP8/FP4/Marlin kernel is gated on **arch-exact** `12.0a;12.1a` (`SCALED_MM` L799, `FP4_SM120` L973, `MARLIN` L580/597/1239, `DSV3_FUSED_A_GEMM` L713, `CUTLASS_MOE_DATA` L942). `a`-targets have **no minor-version forward compat**, so a 12.0 build carries `sm_120a`, not `sm_121a`. **Inference from build config, NOT measured — we have not run the wheel** (H1 imports but fails quantized paths / H2 fails at startup; one throwaway venv settles it). 🔴 **FOUR open GB10/sm_121 bugs filed AFTER both releases:** **#51920** (08-12 04:03Z) `FlashInferMLASparseSM120Impl` never sets `masked_mha_available` while `mla_attention.py:~780` reads it unconditionally -> `AttributeError` -> **engine core dies on startup profiling**, ranks crash-loop under `--restart`; **#51921** engine **stalls after ~1 min idle** on GB10/sm_121 aarch64 (`shm_broadcast`); **#51959** DeepGEMM pin has no SM120 kernels; **#51987** revert of FlashInfer XQA on SM12x. Plus **#51758** (11 comments) `0.26.0 -> 0.27.0 deepseek v4 flash error` — reporter is **x86_64**, so the DS4 regression is **not** GB10-specific. **MLA is DeepSeek's attention: #51920 breaks exactly DS4 Flash, on exactly our chip, in the release being celebrated.** `v0.27.1` is a **single line** (*"Support quantized DSpark Markov heads (#50424)"*) published 08-11 10:47Z — **before** #51920/#51921 existed, so **neither released version fixes this**. ⛔ Do NOT quote *"important SM121 fix"* as meaning the wheel works on GB10. ⛔ Do NOT take the DS4 perf claims (2x kernel, 1.88x, 3.4%/3.9% TTFT) as ours — vendor-claimed, other hardware. ⛔ PyTorch **2.13.0** + Triton **3.7.1** is a breaking env change -> throwaway venv, non-prod port, never `:11434`/`:11435`/`:8889`, never kevin-spark. **Keep separate from `t_7cb87a81`** (TurboQuant) or the results confound. **Sequencing verdict: SGLang ST-14 `t_13d79be5` Phase 0 is free, read-only and has no blocking bug; vLLM has a known startup crash. If only one engine gets a window, start with SGLang.** Also open: **#51655 Muse Glimmer support** — not in vLLM yet |
| **Search the vendor's own issue tracker for YOUR arch before spending a window** — release notes are marketing, the issue list is the truth | SIG-20260812-03 (method) | **P1** | The v0.27.0 notes advertise the SM121 build fix; the issue tracker, filed 24-48 h later, carries an unfixed **startup crash on sm_121**. One search (`repo:vllm-project/vllm GB10` -> **411 results**, `"DGX Spark"` -> **401**) inverted the recommendation for free, before any download. Add to the bench contract: **before a spike consumes a consumer-paused window, grep the upstream tracker for the arch string and the model family, and record the open-bug count + the newest one's date in the bench row.** Compounds with `pct_of_reference` (SIG-20260811-05) and prefix-hit-by-turn (SIG-20260812-01) — all three are provenance fields a result is void without | **folded into `t_6e058ca2`** (bench provenance), not carded separately |
| **The engine-phase candidate that CLEARS Ryan's supply-chain veto — and it ships the diagnosis of our own KV pathology** | SIG-20260812-01 SGLang Unified Radix Cache (sgl_project → LMSYS blog) | **P0** | **Provenance read live, not inferred:** `sgl-project/sglang` **★31,733 Apache-2.0**, tag **v0.5.17** (08-08); `mem_cache/unified_radix_cache.py` returns **HTTP 200 at the TAG** (not main-only); `server_args.py:1433 enable_session_radix_cache`; **official PyPI `sglang-0.5.17-cp312-cp312-manylinux_2_34_aarch64.whl`** + `sgl_kernel-0.3.21-cp310-**abi3**-…aarch64.whl`; `load_format="gguf"` present; **`deepseek_v4.py` + `deepseek_v4_dspark.py` + `deepseek_v4_nextn.py` all in-tree**; sm121 **actively maintained** (#32330, #31366 closed 08-12; **#19637 "SM120 Performance Optimization Plan" OPEN**). ryan-spark measured: `aarch64` · Py **3.12.3** · compute_cap **12.1**=sm121 · 3.3 TB free · neither sglang nor vllm installed — **the wheel triple-matches the box**. Their session-eviction rationale is *verbatim* our measured pathology: *"LRU… can evict an active session's KV while retaining unrelated entries"* vs our **95.4% of evicted DS4 KV entries with `hits=0`**, ~38% hit rate, `reason=disk-cache-full`. Mechanism, not tuning: **DeepSeek-V4 composes FULL + SWA** (whole-prefix reuse vs contiguous trailing window), so DS4's **single-boundary disk checkpoint** must either discard valid reuse or permit invalid reuse. | **CARDED ST-14 `t_13d79be5`** (steals board, blocked: consumer-paused window on ryan-spark; **Phase 0 — import `sgl_kernel`, confirm sm121 — is free/read-only and releasable alone**). **Supersedes the vLLM route on provenance:** `t_08e96127` carries a `block_loop_detected` STOP — *"Ryan countermanded this 2026-08-11: 'dont install something with only 10 downloads.' The earlier supply-chain OK on this card is REVOKED."* Verified live: **no wheel and no vllm ever landed on spark.** ⛔ **Do NOT quote "145.5K vs 9.4K" as 15×** — that is *effective input-token throughput* from their own `bench_multiturn.py`, which **credits cache-hit prefix tokens**; it is serving progress under reuse, not prefill compute. ⛔ Their SWE-bench deltas (TTFT −2.9% to −16.6%; device hit 42→51% DSv4-Pro bs128, ~5→34% Qwen bs32) are **not an isolated ablation** — the blog states it changes cache impl **and** policy together. ⛔ **Nothing transfers**: 4×/8× H200 TP4/TP8 + a **500 GiB Mooncake** L3 tier vs one 121 GB GB10. ⛔ Never on `kevin-spark` (DS4 admission needs ≥105 GiB and DS4 holds ~105). Never bind `:11434`/`:11435`/`:8889`. **Keep separate from `t_7cb87a81`** (TurboQuant = context/KV; this = cache policy/width) or both results are confounded. **Seventh independent signal on the same prerequisite** — Latent Space · McNab width · Entrpi fork · DFlash · OoO-Spec · TurboQuant · this: B17's engine phase is the gate, not one experiment among many |
| **Report prefix cache hit rate BY TURN, not aggregate** — the round-indexed curve is what exposes the capacity cliff | SIG-20260812-01 (their Figure 4 methodology) | **P1** | Their multi-turn figure plots hit rate + TTFT **per round**, which is why the L1/L2 capacity cliff is visible at all; an aggregate number hides it entirely. Our DS4 KV reporting is aggregate-only (`hits=`, `evicted`, `reason=disk-cache-full`), which is why the 64 G → 512 G raise looked like a clean win when the working set may simply have moved the cliff later. Add **prefix-hit-rate by turn** + **which tier served it** to the bench contract | **folded into `t_6e058ca2`** (bench provenance), not carded separately. Compounds with the existing `pct_of_reference` steal from SIG-20260811-05 — a hit-rate row without a turn index is the cache-side equivalent of a tok/s row without a named ceiling |
| **We route per-LANE and never per-TURN — and the signals for per-turn are already on disk** | SIG-20260811-06 NeMo Switchyard `stage_router` | **P0** | Their scorer keys on **tool-result history**: `severity` (windowed error severity), `spinning` (deep churn, no reads/writes), `exploring` (reading/planning without producing) → **capable**; `recent_production_intensity` (writes/edits landing) → **efficient**. Signed score `tanh`-squashed to `[0,1]`, **corroborative** — one full signal scores **~0.46**, just under the `0.5` bar, so a second signal is what decides; **critical-error severity is a hard override**. At `confidence_threshold = 0.0` there is explicitly **no per-turn LLM call**. Our `~/.t1000/routing/` (5,075 LOC, SR-1…SR-8) answers *"which model owns lane X"* from offline evidence, applied once at dispatch — **zero per-turn routing exists**. Port the scorer as a read-only **observer** over `task_events` + tool results, log the verdict it *would* have made, change nothing. **Not theoretical: `t_d68e505a` burned its circuit breaker twice on "iteration budget exhausted 90/90" — textbook `spinning`, no escalation, sole parent of three stalled cards.** | **CARDED `t_95bb6eb2` (ST-13, steals board, blocked: Ryan's scope word)**. ⛔ **Pattern only — do NOT install Switchyard**: vendor labels it *"pre-alpha… not for production"*, it is a Rust/cargo proxy, and it would be a **second control plane** in front of a tested router we own. ⛔ Its quickstart (`switchyard launch claude` + `OPENROUTER_API_KEY`) bills per-token **in front of a Max subscription that runs via claude-acp** — the exact key-fallback the desk cost rule forbids. ⛔ Their own known-issue 0.2.0 #2: routing-tier attribution is **missing from `/v1/stats` and `/metrics`** for escalation decisions and `stage_router` fallbacks — unauditable, same class as the DS4 `ExecStartPre` that verifies a receipt instead of a binary. **Do not carry their `0.5`** (calibrated on SWE-Bench Pro Python-75) or reach for `capable_first` (**their docs: no calibrated thresholds, no measured figures — every published result is `efficient_first`**) |
| **RESCUE/LOSS quadrant calibration — the counterfactual arm our scoreboards have NEVER run** | SIG-20260811-06 Switchyard threshold-calibration recipe | **P1** | Minimum-data path: **~40–75 pure-capable tasks + ~20 pure-efficient probes**, stratified across easy+clean / easy+tricky / hard+structural / hard+localized, then `RESCUE = capable-fail ∩ efficient-pass` (escalation pays) · `LOSS = capable-pass ∩ efficient-fail` (do NOT escalate) · `SAFE` · `HARD`; pick the **lowest** threshold that rescues RESCUE without over-escalating LOSS. **This is the instrument `standings.py` lacks** — Wilson lower bounds rank models *within* an instrument, but nothing measures where escalation **pays**. LAB-SCOREBOARD holds **421 rows of "what the live model did" and 0 rows of "what the cheaper model would have done on the same task."** Build on an **existing** instrument (golden / repair-battery), never a new one | **folded into `t_95bb6eb2` as Phase 2**, explicitly gated behind Phase 1's observer report. Their honest caveat carried over: in stage-router the efficient model inherits partial context, so RESCUE measured from *pure*-efficient runs is a **conservative lower bound**. Third steal (S3) logged not carded — **fail-open judge contract**: buffer the weak reply, judge the **completed** turn (not predicted difficulty), latch only after N consecutive confirmations, and on judge timeout/error/unparseable **serve the buffered cheap reply and HOLD the streak** — never escalate, never reset. Adopt as the design contract on `t_c5510683` (ST-03 approval gate) |
| 🔴 **LIVE REGRESSION — the fork cutover on `kevin-spark` is HALF-APPLIED and we are running the documented rollback binary at ~half the measured decode** | SIG-20260810-07 — **found while baselining decode for the TurboQuant comparison** | **P0** | Read live: `ExecStartPre=/usr/bin/grep -Fxq source_commit=3b792c9a…` greps the **fork** commit **and passes** (the receipt *was* updated), but `ExecStart=/srv/ryan-lab/ds4/app/ds4-server` — which the fork receipt itself names as `rollback=… (upstream b0309611 binary retained, same flags no spec env)`. Binary sizes/dates confirm it: `app/` 41,427,752 B **Aug 8 08:56** (upstream receipt `2026-08-08T09:59:27`) vs `fork-src/` 36,145,536 B **Aug 9 22:44** (fork receipt `2026-08-09T22:53:34`). Running PID **4072645** = `app/`. **Spec env absent** — unit sets only `HOME`+`CUDA_CACHE_PATH`, no `DS4_CONT_MTP_MODE=2`/`DS4_CONT_DSPARK=1`. Journal decode under real tool-call load @~60k ctx: **13.0–13.7 t/s** vs the receipt's banked **25–26**. **Receipt+gate moved to the fork; binary+env did not.** | **commented `t_43e997d2` + `t_d1947f46`, no card** (board 48 blocked). ⛔ **Do NOT `systemctl restart ds4`** — that relaunches the same rollback binary. Fix = drop-in pointing `ExecStart` at `fork-src/ds4-server` + the two `DS4_CONT_*` `Environment` lines; **engine change on Kevin's box, needs Ryan's explicit OK.** Structural steal: **`ExecStartPre` should assert the artifact (path + sha256 of the binary it launches), not grep a receipt file** — a supervisor that verifies a receipt is not verifying a binary |
| **A published 16×-context path exists for the EXACT model we serve, on the EXACT silicon we own — and it is gated on an engine migration, not on hardware** | SIG-20260810-07 TurboQuant `TQ3_4S` (coffeecup2020 → YTan2000 / turbo-tan) | **P0** | `R2-DeepSeek-V4-Flash-0731-TQ3_4S`: **91 GiB @ 2.73 bpw serving 1,048,576 ctx on a single DGX Spark GB10 at 18.4 tok/s (22.9 + DSpark)**, compressed KV `-ctk q4_0 -ctv tq3_0` ⇒ ~14 GB for 1M. We are at **65,536** after `t_01ac807e`; this is **16× beyond** at equal-or-better decode. Fork verified real: `turbo-tan/llama.cpp-tq3` **222★ MIT, explicit CUDA kernels**, pushed 08-10 20:11Z; author has **17 models** (top 2,730 dl) and this is his **second** pass at our model (prior 110 GB/512K has 320 dl). Their "stock llama.cpp cannot load it" is **independently confirmed** — ryan-spark `62bf73d` `ggml.h` has only `TQ1_0=34`/`TQ2_0=35`, and `grep -ril turboquant\|TQ3_4` → 0 hits. Spike on **Ryan's** Spark, stepped ctx 262k→524k→1M. | **CARDED `t_7cb87a81`** (blocked: window + fork-build OK + 97 GB download). ⛔ **The 1M trick is NOT portable to our DS4 lane — verified live that `ds4-server --help` has NO KV-cache-dtype flag at all** (its KV section is disk-checkpoint only; `--kv-cache-reject-different-quant` is about *routed-expert* quant). Reaching 1M means **replacing `antirez/ds4`** — a **third** independent argument that B17's engine phase is the gate. **Keep separate from `t_08e96127`** (vLLM = width/prefill; tq3 = context/KV — benching together confounds both). Never on kevin-spark: 91 GiB + ~14 GB KV ≈ 105 GiB vs DS4's existing 105.1 of 121 = displacement. **Quality is NOT free** — their own table has R2 *worse* on HumanEval (90.9 vs 94.5) and HumanEval+ (86.6 vs 90.9) than the larger predecessor, better on Hard86 (76 vs 70) |
| **The untested KV step is `q8_0 → q4_0`** — *not* "we've never quantized KV" | SIG-20260810-07 (found while checking TQ3 portability; **self-corrected within minutes by a damage check**) | **P1** | ⚠️ **I first filed this as "KV quantization is a knob we own and have NEVER turned." FALSE — falsified live.** `pgrep -af llama-server` on ryan-spark shows Ollama's bundled runner already at `--cache-type-k q8_0 --cache-type-v q8_0 --flash-attn on`, because the unit sets **`OLLAMA_KV_CACHE_TYPE=q8_0` + `OLLAMA_FLASH_ATTENTION=1`** globally — and **our own `~/llama-ab-start.sh:13-14` sets q8_0 too**, so `t_227d09b2`'s 49–50 tok/s was never an f16-KV run. **Corrected steal:** we sit at `q8_0`; TurboQuant runs **`-ctk q4_0 -ctv tq3_0`**. Bench `q8_0` (control) vs `q4_0`, `-fa on` held constant, golden-gated — report decode · prefill · peak resident · **max ctx that fits** · pass rate. It prices the *quality* half of the 1M claim for free. | **folded into `t_7cb87a81` as Phase 0** — deliberately *first*, because if `q4_0` K degrades quality on our traffic then TQ3_4S's even-more-aggressive `tq3_0` **V**-cache is in doubt and the 97 GB + fork build should not be spent. **Phase 0 needs only a window** (no supply-chain OK) and can be released alone. **Bonus fleet facts banked, neither previously written down:** per-slot ctx already served is **131,072** (`gpt-oss:120b`, `-c 262144 -np 2`) and **16,384** (`hermes3:8b-16k`, `-c 32768 -np 2`); and **`OLLAMA_KEEP_ALIVE=-1` is set globally in the unit**, so *every* model Ollama loads is pinned Forever — that is the mechanism behind the 08-09 memory jam (~6 GB available), not a per-model choice |
| ⚠️ **SPEC-DEC HAS A POSITIVE RESULT AND WE DID NOT KNOW** — the Entrpi fork cutover on `kevin-spark` **already happened** (2026-08-09 22:53), with a DSpark drafter live | SIG-20260808-06 Entrpi fork — **found while verifying SIG-20260810-06** | **P0** | Read live off `kevin-spark`: `INSTALL-RECEIPT.txt` rewritten to `engine=Entrpi/ds4 fork · source_tag=v0.5.6.1 · commit 3b792c9a · build_target=cuda-spark (sm_121a)`, and the running process (PID 3555931, `/srv/ryan-lab/ds4/fork-src/ds4-server`) carries `DS4_CONT_MTP_MODE=2 DS4_CONT_DSPARK=1 DS4_DSPARK_MODEL=…DSpark-drafter-Q2K-Q8-0731.gguf`. **Receipt records `bench_decode=25-26 tok/s (was 16.07 upstream)` and `dspark accept=72-73% tok_per_step=2.83-3.00`.** That is a **~1.6× decode win with healthy acceptance on CUDA** — the first clean positive datapoint in the family. | **SUPERSEDES the "three spec-dec losses" framing in the OoO-Spec row below.** `t_716141c4`'s NEGATIVE-FINAL was true of **upstream** (Metal-only, 0 CUDA) — the **fork ships a CUDA DSpark path** and it works. ~~**⛔ OPS RISK: `ds4.service` is `inactive/dead`** — the live server is an unsupervised manual launch, and the unit's `ExecStartPre` still pins upstream `source_commit=b0309611` so **`systemctl start ds4` now fails its own receipt gate**. No supervisor, no journal.~~ **← STALE, CORRECTED 2026-08-10 ~19:00 (SIG-20260810-07): the situation INVERTED and got worse, not better. `ds4.service` is now `active/running` (start `Mon 2026-08-10 18:49:26 EDT`, `NRestarts=21`) and the receipt gate now greps the **fork** commit `3b792c9a` and **PASSES** — but `ExecStart` still launches `app/ds4-server`, the binary the fork receipt itself names as the upstream **rollback**, with no `DS4_CONT_*` spec env. So we traded "unsupervised fork at 25–26 tok/s" for "supervised upstream at 13.0–13.7 tok/s." Supervision was restored onto the wrong artifact, which makes the loss durable. See the LIVE REGRESSION row at the top of this table.** Cross-ref `t_43e997d2` (was blocked *proposing* this), `t_d1947f46` (K5 supervision, regressed), `t_6e058ca2` (provenance) |
| **We own the measured KV/token for DS4 Flash, and it is 3.2× below the number being published** | SIG-20260810-06 danpacary KV disagg | **P1** | He projects **~44 KB/tok** (q8, "kv geometry from model configs") for the exact model we serve; 17 paired `(tokens, size)` journal lines fit **`size_MiB = 22.85 + 0.013131 × tokens`** — **13.77 KB/tok marginal + 22.9 MiB fixed header**, max residual 0.04 MiB. His 500k headline (22.5 GB / 4 min wifi / 20 s wire) measures as **7.07 GB / ~75 s / ~6.4 s**. Cause: **MLA stores a compressed latent, not config-width K/V** — the same over-prediction `t_01ac807e` hit (+0.67 GiB where a doubling was predicted). Second finding: the fixed header makes short contexts pathological (**56 KB/tok effective at 563 tokens**) — shipping only pays at long context. | **open — no card, deliberately.** The experiment is **not reproducible on our fleet**: his rig is M4 Max **128 GB**, ours is M3 Max **64 GB**, and `ds4flash.gguf` is **86.7 GB** — byte-identical-weights-on-both-boxes fails on RAM. Banked as fleet fact for cache budgeting + any future disagg (Phase-D mini at 128 GB would revive it). **⚠️ Our 13.77 figure is an *upstream-build* measurement (PID 3384230, 08-09) — the fork changed the engine and nobody has re-measured.** Adopt his pre-registered gate wording (*"99% token-identical vs local prefill, correctness before speed"*) → `t_6e058ca2` |
| **B18 has data for two training rungs it never planned — and one trap that would burn the referee** | SIG-20260810-05 Liquid4All/cookbook (via Leonie) | **P1** | Their `grpo_for_verifiable_tasks` + `dpo_with_trl` recipes map onto assets we already collect: **`score_case()` (`juice/evals/scorers.py:218`) returns 4 deterministic component scores — a verifiable GRPO reward in all but name**; and each outcome-loop row already carries the model's `classification` (rejected) *plus* Ryan's `label.correction` (chosen) — **a DPO pair**. So the ladder is SFT (teacher distill, planned) → DPO (Ryan's calibration) → GRPO (**needs zero human labels**, which matters when labels sit at 0). Retroactive: the Option-B storage decision made the corpus DPO-ready by accident. **⛔ TRAP: never GRPO against `golden_set_research_v2.json` — it is the referee; training on it destroys the only instrument that can grade the student.** | **CARDED `t_de3a5688`** (blocked: one word — plan patch only, no training run). B18 is corpus-blocked (**2/500** pointers, **0** labels) so none of this is reachable — a cookbook is not progress. **`license: None` = all rights reserved: read, do not copy.** Unsloth is CUDA → routes to Caden (RTX 4060 **8 GB**, QLoRA-only), not the MLX MBP/mini path |
| **A prebuilt vLLM wheel for GB10 already exists — the B17 engine phase may be a download, not a build** | SIG-20260810-04 ExoLabs `exo-spark-assets` | **P0** | `vllm-0.23.0rc2+g47bd77088.**sm121-cp312-cp312-linux_aarch64**.whl` (147 MB) + `exo-warm-cache.tar.gz` (75 MB, prebuilt `torch.compile` cache). **Triple checked LIVE against ryan-spark, not inferred from the filename**: compute_cap **12.1**=sm121 · Python **3.12.3**=cp312 · **aarch64** · vllm not installed · 3.3 TB free. Compounds with `SIG-20260810-03`: vLLM beat llama.cpp on **prefill 3.8–8.5× at `max-num-seqs=1`** — single-stream, and prefill is our worker bottleneck. Bounded spike: fresh venv, non-production port, repair-battery + prefill vs llama.cpp's banked 49–50 tok/s / ~1,070 tok/s prefill. | 🛑 **COUNTERMANDED 2026-08-11 — this route is DEAD, do not re-propose it.** `t_08e96127` sits in `triage` with a `block_loop_detected` event: *"STOP — Ryan countermanded this 2026-08-11: 'dont install something with only 10 downloads.' The earlier supply-chain OK on this card is REVOKED."* **Verified live 2026-08-12: no `.whl` and no `vllm` ever landed on ryan-spark** — the veto held before anything was fetched. **The engine-phase GOAL survives; this artifact does not.** → replaced by **SIG-20260812-01 / ST-14 `t_13d79be5` (SGLang)**, which clears exactly this objection: first-party PyPI wheel, 31.7k★, Apache-2.0, sm121 issues closed the same week. Original card notes retained below for the record. Also commented onto `t_99c5d345` (B17). **Supply-chain, not package-manager**: release candidate at a pinned git hash, **10 downloads**, ~2 months old, and **`exo-spark`'s source is unpublished** — the assets repo cites a repo that doesn't exist in the org. Ryan's Spark only (never Kevin's — DS4 admission needs ≥105 GiB free); never `:11434` (Juice teacher contract) or `:8889`; rollback = delete the venv. **↑ Updated 2026-08-12 by SIG-20260812-03 — the veto on THIS artifact stands and is not being re-proposed; what changed is that an *official* vLLM PyPI aarch64 wheel now exists, so the supply-chain objection no longer applies to the vLLM *project*. It is replaced by a runtime objection (#51920 startup crash on sm_121 for MLA models = DS4 Flash), and by the finding that the SM121 fix is a source-build fix — so "download, not a build" is wrong either way. SGLang still goes first.** |
| **Never bench a quantized draft head — and log which draft actually loaded** | SIG-20260810-03 Arc B70 cookbook (SergeB, MIT) | **P0** | Their vLLM patch exists because *"draft inherits target GPTQ quant_config"* silently wrecks the draft head. **Read live off spark: our own EAGLE3 bench ran `eagle3-gpt-oss-120b-Q8_0.gguf` while `eagle3-gpt-oss-120b-BF16.gguf` sat unused in the same directory** — and used `--spec-draft-n-max 16` against a measured mean accept length of 2.8–3.2. We rejected spec-dec on **11.7–14.2%** acceptance vs EAGLE3's published 60–80%. Re-run with the BF16 draft + `n_max 4`: two known-wrong variables, both free, weights already on disk. If it stays ≲20%, we close spec-dec with evidence instead of a confound. | **CARDED `t_99c6388b`** (blocked: consumer-paused window) as Arm A, with the free `ngram-*` arm B alongside. Qualifies `t_227d09b2`'s EAGLE3 row. **Their tok/s do NOT transfer** (Intel Arc / SYCL is llama.cpp's weak backend; ours is CUDA) — the mechanism ports, the numbers don't |
| **Profile before optimizing the wrong segment** — measure what % of a real worker card's wall time is prefill vs reasoning-decode vs tool-call-decode, before spending another spec-dec window | SIG-20260810-02 OoO-Spec (arXiv 2608.00814) | **P0** | **THREE local spec-dec results now say drafters do not transfer cleanly to code**: EAGLE3/120b-llama.cpp 12–14% accept → **2× slower** (`t_227d09b2`); DFlash/muse-glimmer-MBP — drafter tuned for reasoning filler (60–74% accept there), but on real code it barely fires (near-zero draft attempts) and is **still ~32% slower** than plain decode, a structural dual-model tax not an overhead/benefit tradeoff (`t_e28b2c0e`, corrected 2026-08-10); DSpark/DS4 Metal-only no-op (`t_716141c4`) — **⚠️ THIS THIRD ONE IS NOW SUPERSEDED (2026-08-10): that no-op was *upstream*; the Entrpi fork's CUDA DSpark path is live on `kevin-spark` at 72–73% accept / 2.83–3.00 tok per step / 16.07 → 25–26 tok/s. So it is TWO losses and one WIN, and the family should not be closed on the "drafters don't transfer" story.** Meanwhile the one thing we *did* measure on the worker path — the KV raise (`t_e471787f`) — moved **prefill 7.30s→2.47s and decode not at all**. Worker prompts are ~35.7k tokens. Get the segment split, then either bench `ngram-*` (free, no weights) or close the family on the record. | **CARDED `t_171fbfd2`** (mesh, blocked on Ryan: pick the target card). Consumes MESH-TEL-2's stream if `t_3a1b8b6b` lands; one-shot manual profile explicitly in scope otherwise. Their 3.89× is 2×H100/vLLM, partly in-distribution (LoRA trained on API-Bank+ToolAlpaca, evaluated on the same two + BFCL), **no code released** |
| ~~**The serving engine is the gate, not the technique**~~ — *"Ollama exposes ZERO spec-dec surface, nothing in the fleet can consume a drafter"* | SIG-20260810-02 | ~~P1~~ | **SUPERSEDED SAME DAY.** True of Ollama, **false of the fleet**: `t_227d09b2` landed **llama.cpp `62bf73d`** on ryan-spark at 09:59 (entry filed 10:57) with `--spec-type none,draft-simple,draft-eagle3,draft-mtp,draft-dflash,draft-dspark,ngram-*` plus the whole `--spec-draft-*` family. Verified live. The gate moved from *engine* → *accept rate × decode share*. B17's engine phase is still justified for **continuous batching / width** (llama.cpp doesn't give us that) but no longer blocks spec-dec. | superseded — correction written into the entry §2/§3 and commented onto `t_99c5d345` + `t_227d09b2` |
| **Spec-dec shipped as a pull-able Ollama tag** (`muse-glimmer:30b-nvfp4-dflash`, **+2 GB** over the plain tag) — a drafter co-shipped with the weights instead of a build flag | SIG-20260810-01 Unsloth / Meta Muse Glimmer | **P0** | One-variable A/B: `30b-nvfp4` vs `30b-nvfp4-dflash`, identical prompt, hold `reasoning_effort` constant — report **tok/s + tokens/step + accept %**. Answers *"does spec-dec help on GB10 at all"*, which `t_716141c4` left open when upstream DSpark turned out Metal-only. **No fork, no Kevin, no restart.** | ✅ **CLOSED 2026-08-10, corrected same day** `t_e28b2c0e` — ryan-spark (0.31.2) NEGATIVE-FINAL, whole-model-family manifest gate ("may be in pre-release"). Full A/B ran instead on Ryan's MBP (upgraded 0.23.0→0.32.7). First pass used flawed wall-clock timing + reasoning silently eating the token budget — Ryan caught it against published community numbers. Corrected via native eval_count/eval_duration + think:false: baseline **20.7 tok/s** (not 15.0). Drafter barely fires for code at all (near-zero draft attempts, vs 60-74% acceptance when reasoning is on) yet decode is still **~32% SLOWER** (14.0 tok/s) — a structural dual-model tax, not an acceptance/overhead tradeoff. dflash tag removed; plain nvfp4 kept as the fleet's first working local vision model (vision smoke test passed) |
| **Re-run the DFlash A/B under llama.cpp, not Ollama** — an independent RTX 4090 bench gets **50 → 75 tok/s (+50%)** from the *same* drafter, the opposite sign to our −32% | SIG-20260810-01 §9 (analogalok) | **P0** | Our arm: Ollama/Metal on the MBP, `n_max` **not settable** (Ollama exposes no spec knobs). His: CUDA llama.cpp, `--spec-type draft-dflash --spec-draft-n-max 3`, `-b/-ub 4096`. **Verified live: ryan-spark already has `~/llama.cpp` `62bf73d` = "model: Muse Glimmer Support (#26841)", CUDA-built, `llama-server` advertising `draft-dflash` + `draft-dspark` + 4 `ngram-*`, `n_max` default 3.** Bypasses the HTTP 412 manifest gate entirely; needs only the ~17 GB GGUF + 1.6 GB drafter. Fits the ratified **Option 1** split (llama.cpp = window/batch engine). ⚠️ Not an engine-vs-Ollama story — llama.cpp **also** lost on EAGLE3 (`t_227d09b2`, 12–14% accept, 2× slower). Six variables differ; the one we *know* we set wrong is **`n_max` 16 vs mean accept len 2.8–3.2** | **open** — folded into `t_99c6388b` (blocked on a consumer-paused window) |
| **Never bench spec-dec through an engine that hides `n_max`** — Ollama's `-dflash` tag is a black box; both published wins on this drafter set `n_max` explicitly | SIG-20260810-01 §9 | P1 | Add to the bench-provenance contract alongside the existing draft-file/dtype/quant fields. A spec-dec row whose `n_max` is "whatever the engine chose" cannot be compared to anything | **open** — → `t_6e058ca2` |
| **Execution-feedback repair pass** (run tests → feed failures back → one retry) — claimed to beat 6× memory | SIG-20260809-01 danpacary | **P0** | Extend the G3 runner with a test-exec step + **3 arms incl. blind-retry control**; measure before spending the fork lever | **carded 2026-08-09** `t_6506193d` (blocked: Ryan window on his Spark) |
| **Factorial harness-vs-model design** (vary ONE factor at a time, report both deltas) — their result: **framework alone +30 pts, model alone +20** | SIG-20260809-02 Frontis-MA1 / OpenRSI | **P1** | Adopt as the report shape for the repair-pass A/B — it *is* the control structure that card already demands | **wired 2026-08-09** — commented onto `t_6506193d`; no new card |
| **Compile the index, don't hand-maintain it** — generate `INDEX.md` from `entries/` frontmatter | SIG-20260809-03 OpenKB | **P1** | Landed as `scripts/signal_log_index.py` (new file, not a patch to the scaffold — the scaffold was one of the two writers). Bare invocation diffs (**no `--check` flag** — that errors), `--write` regenerates | ✅ **SHIPPED 2026-08-09** — generator reproduced all **14/14** hand-curated rows byte-identically before cutover; `signal_log_new.py` no longer writes its own row. **Doc-index §7 is still hand-maintained — not covered by this** |
| **Reasoning models silently fail strict-schema jobs** — prefer non-reasoning coder, assert non-empty output not exit code | SIG-20260809-03 spike | **P0** | 3rd instance (DS4 Flash, gpt-oss small-budget, gpt-oss/OpenKB blank-pages-with-`[OK]`). qwen3-coder:30b passed the identical job. Hand-probes give false PASS | **shipped — `t1000-model-desk` doctrine** |
| **Write measurement results back into the signal entry, not only the card** | SIG-20260809-03 spike | **P1** | Compiled `agent-width` page asserted McNab's 62.5 agg tok/s as demonstrated — our disconfirming N=1→4 ladder lives only on `t_9c7208c2`. A compiled wiki propagates disproven claims in confident prose | open — log only |
| **Raw → compiled is a real layer; session logs are not it** | SIG-20260809-03 OpenKB | P1 | KRT `.omc/wiki` is **581 session-logs / 22 curated of 604**; `llm-wiki` skill exists but `WIKI_PATH` unset and never run; `docs/research/` (195 md) has no wiki. Add a compile pass before more auto-capture | open — log only |
| Named operator set **Draft / Improve / Debug / Crossover** as code-lane vocabulary | SIG-20260809-02 Frontis-MA1 | P1 | We have Draft (G3) + Debug (repair, carded). **Improve** and **Crossover** have no DevBot analogue | open — log only, board at 46 blocked |
| Execution-grounded labels — environment scores the trace, no human thumb | SIG-20260809-02 Frontis-MA1 | P1 | B18 is at **0 labels / 2 pointers**. Split the scorer: schema-valid / evidence-grounded / refusal-correct are **checkable today**; intent / urgency / followup have **no oracle** and stay Ryan's | open — do NOT read as "stop labeling" |
| **DS4 gap is DECODE-ONLY — 16.07 vs 28.6 tok/s; prefill already at parity** | SIG-20260808-06 DwarfStar/Entrpi | **P0** | Propose Entrpi fork cutover **to Kevin** (his box, no install) | **carded** `t_43e997d2` — **upstream-DSpark precondition now SATISFIED (negative)**; blocked on Kevin OK only. W2 measured our prefill at **~1,070 tok/s, matching the fork's claim**, so the fork lever is decode, not prefill |
| DS4 speculation + continuous batching as the decode/width lever | SIG-20260808-06 DwarfStar/Entrpi | P1 | Enable `--dspark/--mtp`; width ladders both boxes | ✅ **ANSWERED — negative** `t_716141c4` done 08-09: upstream DSpark is **Metal-only** (159 metal-tagged lines, 0 cuda), A/B 16.23 vs 16.39 = 1.01× noise, rolled back. **Fork is the only spec-dec route on GB10 — do not re-run upstream.** Width: `t_9c7208c2` (Ryan N) · `t_89c72b32` (Kevin N, ack-gated); R12 says `KEEP_ALIVE=-1` eviction, not width, is the Ryan-box bottleneck |
| Concurrency bench N={1,8,16,32}, agg tok/s, width vs 120B depth | SIG-20260807-03 McNab | P0 | B17 **Phase C2** | **wired** plan + `t_fd210e86` decomposed into the two ladders above |
| Spec-dec + TTFT≠decode + prefix/KV sacred | SIG-20260806-01 LS masterclass | P0 | B17 Phases C/D; cache discipline | ✅ **wired + executed** `t_01ac807e` done 08-09: ctx 32768→65536, all 3 configs lockstepped, 58,357-tok prompt → 200. **MLA compresses KV hard — +0.67 GiB for +32k, ~21 KiB/token all-in**, not the doubling I predicted. Residual: `kv-disk-space-mb` still 8192 with `disk-cache-full` evictions in journal — own restart, cheap win |
| **Parent strong / subagents cheap-fast** | SIG-20260808-02 codex-router | P1 | Child model ≠ parent by default | ✅ **SHIPPED** — B22: `orchestrator`→Flash@kevin-spark, `worker`→120b@spark; per-card `model_override`/`provider_override` |
| Picker only lists authed/live routes | SIG-20260808-02 codex-router | P1 | Don't surface dead Spark/Ollama aliases | ✅ **SHIPPED** `t_cf82f21c` (phantom ds4-pro aliases deleted) · residual `t_3084469d` (strip from skill doc) |
| Code-lane turn/token/time budgets + test gate | SIG-20260807-02 Prime | P1 | Coding agent autonomous limits | ✅ **likely already shipped** — `max_runtime_seconds`, `max_retries`, `consecutive_failures` live in the tasks schema. **Verify before building.** |
| Idempotent job identity + lost-response reconciliation | SIG-20260808-05 Agent Dock | P1 | No orphan or duplicate dispatch | ✅ **likely already shipped** — `idempotency_key`, `current_run_id`, `last_heartbeat_at` in schema. **Verify before building.** |
| **Spark serve metric card** (util, mem, tok/s, TTFT p95, KV%) | SIG-20260808-04 sparkDash | P1 | Results template + ops | 🟡 **partial** — `FLASH-SCOREBOARD.md/.jsonl` live, `t_cf7fd6cd` done; gap open in `t_d0277c0b` (tok/s never populated, M2) |
| Capability→workflow→model verb map | SIG-20260807-04 OMH | P1 | Router aliases (`default/private/code/format`) | 🟡 **partial** — `t_d754cc37` lane migration via scoreboard standings (todo); alias hygiene shipped |
| **Capability-aware owner routing** — refuse an owner that can't meet the card's floor | SIG-20260807-04 OMH **v1.0.5** | P1 | Pre-dispatch check vs `MINIMUM_CONTEXT_LENGTH`; would have caught Flash 9/9-eval-vs-1/13-worker | **open — no card** (adjacent, now closed: `t_59505920` diagnosed it by hand) |
| **Capability projection** — send only the tools a request needs | SIG-20260807-04 OMH **v1.0.5** | P1 | Lower the 35.7k worker-prompt floor (complement to W2's ceiling raise, `t_01ac807e` done) | **open — no card** |
| **Restart-durable approval gate** (`decision_gate/v1`) | SIG-20260807-04 OMH **v1.0.5** | P1 | Gate survives dispatcher restart + `recompute_ready()` promotion; today needs a sticky-block comment as a workaround | **open — no card** (workaround live on `t_b02284c0`) |
| Swarm isolation: worktree/lease per worker | SIG-20260807-03 McNab | P1 | Policy before multi-agent coding | 🟡 **partial** — B17 C2.4 + `workspace_kind`/`claim_lock`/`branch_name` in schema |
| **Sticky model mid-thread** (avoid model-hop prefill tax) | SIG-20260808-03 NVIDIA KV transfer | P1 | Don't bounce models mid-session unless worth it | **carded 2026-08-08** `t_c9399728` (blocked: Ryan unblock) |
| **Per-profile side channel while orchestrator busy** | SIG-20260808-05 Agent Dock | P1 | Reach `worker`/`orchestrator` without hijacking default thread | **carded 2026-08-08** `t_647eb43e` (blocked: spike vs native vs drop) |
| Explicit assign-task gate (chat ≠ kanban card) | SIG-20260808-05 Agent Dock | P1 | No card minted unless deliberately toggled | **carded** with `t_647eb43e` |
| Settle captured work as `blocked / needs_input`, never auto-done | SIG-20260808-05 Agent Dock | P1 | Matches HUMAN=Ryan / GREEN-only-on-OK | **carded** with `t_647eb43e` |
| `/refine`-style trajectory→skill patch + diff/accept | SIG-20260807-02 Prime | P1 | skill_manage propose with rollback | **carded 2026-08-08** `t_b6d1a13d` (blocked: Ryan decides) |
| Align C2 fields with multi-concurrency decode bench style | SIG-20260808-04 sparkDash | P1 | bench_concurrency metrics parity | **open — no card** (fold into C2 when the ladders report) |
| Evidence gates / clarify-before-build | SIG-20260807-04 OMH | P1 | "what happened / didn't" in workflows | **open — no card** (adjacent: `t_8b3c9a73` intake-row rule) |
| Same-family cascade skip-reprefill (when engines support) | SIG-20260808-03 NVIDIA KV transfer | P1 | Watch vLLM/SGLang; not DIY maps | watch — no card wanted |
| **Adviser-rung escalation** — local model keeps the loop; frontier returns ONE gated, PII-screened, TEXT-ONLY answer. Vendor TB2.1: local 59.6 @ ~$0 → +adviser 73.0 @ ~$0.415 vs frontier-alone 82.4 @ ~$0.65 (~60% of the gain, all context stays local) | SIG-20260825-01 Perplexity Portable Computer | P1 | Design note: rung placement in the ladder + trigger + distillation/preflight + approval UX + fail-closed; decide fork vs upstream-PR to `hermes-agent` | **carded 2026-08-25** `t_a133f091` ST-17 (blocked: Ryan picks lane) |
| **Harness teardown vs hermes-agent (our upstream)** — their harness on stock 27B beats hermes-agent 82.6 vs 74.0 (vendor bench): context rationing, compact CLI tool defs vs MCP, skills load/unload, fail-closed sandbox, PII preflight | SIG-20260825-01 Perplexity Portable Computer | P1 | Phase (a) read-only delta enumeration vs upstream code; phase (b) install the deb on ryan-spark (meets spec exactly) and read the harness on disk | **carded 2026-08-25** `t_610d4184` ST-16 (blocked: Ryan — Pro/Max sub + Spark memory window, 120b resident) |

## Backfill debt

The log starts **2026-08-06**. Six research links pasted before that were never captured;
three of them were *actioned anyway* (Herald cutover, Cerebras→operator-surface crucible,
Adamation/TotalAgent→competitive intel + Kevin inbox 168) and read as gaps only because the log
did not exist yet. One is live-relevant: **@miaai_lab 2026-08-05 claims DS4 Flash ~82 tok/s on 2×
Spark**, a third number against our 16.07 measured and Entrpi's 28.6 claimed.
Card: **`t_b5c30927`** (blocked on one scope question — does the log cover competitive intel, or
agent/infra only?).

## Parked (not in rollup)
- **SparDA (`NVlabs/SparDA`, arXiv 2606.04511) — do not build, do not train, do not card.** ★61 Apache-2.0, **submitted 3 Jun 2026** (repo last pushed 06-04; the 08-12 post is not news). Adds a 4th per-layer **Forecast** projection so layer L predicts layer L+1's KV blocks, giving one layer of lookahead to hide the CPU→GPU prefetch, plus one Forecast head per GQA group instead of one per query head. **Three independent hard-nos.** (1) **No checkpoints** — README verbatim: *"SparDA checkpoints are not source files and are not included in the release package. Train Forecast/indexer weights yourself"*; HF `?search=SparDA` → **n=0**; training is single-node multi-GPU on retokenized ProLong, and the paper says MiniCPM4.1-8B@64K *"completes within 48 hours"* on H100s. (2) **Wrong family** — it retrains only the indexer on models **already sparse-pretrained** (MiniCPM4.1-8B/InfLLM-V2, NOSA-8B); nothing on our fleet qualifies, and §5.1 explicitly excludes DeepSeek: they do not compare against DSA accelerators *"which target token-level DSA rather than the block-sparse backbones used here."* (3) 🔴 **The bottleneck does not exist on GB10** — their rigs are H100/A100 behind **PCIe Gen5×16 / Gen4×16** with 2 TB CPU RAM; measured on ryan-spark `Addressing Mode: **ATS**`, `memory.total **[N/A]**`, `free -g` 121 total — **one coherent pool, no host↔device hop to overlap**. "Offload the KV cache to CPU" is a configuration our Spark cannot enter. (4) minor: Docker defaults `CUDA_ARCH_LIST="8.0;9.0"` — sm_121 is an untested CUTLASS+FlashAttention+`infllmv2_cuda_impl` port, the class that produced vLLM's kernel-less SM121 builds. 🪤 **Both tweet numbers are the best cell of a table**: "1.7× decode" is MiniCPM 128K/**batch 8** vs the **CPU-offload** baseline (279.5→471.2) — at **batch 4**, our actual regime, SparDA is **slower** than plain non-offload sparse at 32K (0.85×) and 64K (0.95×); and "5.3×" is SparDA@B64 vs a baseline that **OOMs past B4**, i.e. batch capacity not speed. "+6.5 accuracy" is one cell of one model on a suite where **Dense scores LOWEST** (NOSA-8B reasoning: Dense 41.6 < Sparse 50.7 < SparDA 57.2) — MATH-500 + AIME24/25 (n=30 each) under sampling with a GPT-5.2 judge; aggregate deltas are **+0.3 / +2.3** and the abstract itself says *"matches or slightly improves accuracy."* ✅ **The one durable artifact is a citation**: the paper independently characterises **DeepSeek-V4's CSA+HCA as cutting per-token KV ~10× over V3.2**, corroborating our measured **13.77 KB/tok** for DS4 Flash (SIG-20260810-06) against a third party's projected ~44. **Reusable filter (S2):** if a paper's win is hiding a **PCIe host↔device transfer** (SparDA, InfiniGen, SparseServe, HiSparse), it cannot transfer to GB10 — classify `watch` without a full workup (SIG-20260812-05)
- **REAP: `twaggs88/DeepSeek-V4-Flash-REAP25-DSpark-ds4-GGUF` — never pull.** Base repo `deepseek-ai/DeepSeek-V4-Flash-DSpark-0731` returns **401** (control-tested); card admits the v4 line is **not** pruned; runs **only** on the third-party `pulsar` engine (repo exists, 200 — it is simply a fourth runtime we would adopt sight-unseen) and explicitly states llama.cpp will not accept its tensor formats; `config.json` is `{}` (SIG-20260812-04 §2.5)
- **REAP: the K160 DS4 build (`heath0xFF/DeepSeek-V4-Flash-0731-REAP-GGUF`) — withdrawn by its own maintainer.** Q2_K/Q4_K_M loop without answering; Q2_K and MXFP4 hit GB10 MMQ backend kernel failures under full GPU offload. Use K216 or nothing (SIG-20260812-04 §2.3)
- **REAP: running `llm-compressor` ourselves — not the path.** Calibration needs the full model resident plus a dataset, and the prune→quantize pipeline's natural target is FP8/NVFP4 for **vLLM**, the blocked engine. The community shelf already covers our models; the free move is header range-reads (SIG-20260812-04)
- kimi-k3-in-c product path — watch only (SIG-20260807-01)
- OMH full install / model-pool sprawl — do not adopt
- Prime curl\|sh as desk primary — spike only if ever
- Cloudflare Computer prod dependency — watch/preview only (SIG-20260808-01)
- codex-router curl install / API-key farm — pattern only (SIG-20260808-02)
- Entrpi one-command installer on Kevin's `spark-b01b` — **Kevin's OK required** (third-party inference engine on a box carrying tunnels + 3 GitHub runners; provenance review + INSTALL-RECEIPT/`ExecStartPre` re-pin before any build). ~~`ds4.service` fails admission~~ **stale — corrected 2026-08-09:** the K5 cutover made the unit the real supervisor (`active`, `NRestarts=0`); the admission failure was the pre-cutover state (SIG-20260808-06)
- danpacary's actual numbers (n=2 repair arm, temp 1.0, no blind-retry control) and his unpublished `evalx` — **steal the method, not the measurements** (SIG-20260809-01)
- OpenKB `pip install` into the T1000 venv — throwaway venv only; pulls `litellm`/`markitdown`/`pymupdf`. Never set `PAGEINDEX_API_KEY` (no-keys rule); Workbench `:7566` ships **auth off by default** — loopback or `OPENKB_API_TOKEN`. Skill Factory stays parked until `agent-skills-estate` audits the ~100 existing skills (SIG-20260809-03)
- Agent Dock `install.py` into `~/.t1000` — Windows-verified only + wants an `executive-organization` board; spike on throwaway `--home` only, on Ryan OK (SIG-20260808-05)
- **`Qwen3.8-2.4T-A95B` in any quant — HARD NO, not a judgement call.** ✅ **RELEASED 2026-08-12 15:59Z** (flagship only). Unsloth's headline **397.3 GB `UD-Q1_0`** (verified: 4.892 TB BF16 → −91.9%) needs **≥450 GB RAM per their own guide** (the tweet's "410GB+" is the looser of the vendor's two numbers); our whole fleet sums to **~322 GB across four non-clustered hosts**, and disk-offload arithmetic lands near **0.2–0.5 tok/s** (95B active × 1.1875 bpw ≈ 14.1 GB touched/token). Their own B200 figure is ~20 tok/s. 🪤 **The 397 GB rung is the most degraded on their own ladder** — PPL +74%, KLD ~2×, top-p −12.6 pts vs IQ1_S: *record the rung, not the byte count*. 🪤 **`62bf73d` has `GGML_TYPE_Q1_0` CUDA kernels but `QK1_0=128` vs their implied 256-weight/38 B block — format identity UNCONFIRMED**, and they admit reusing `TQ1_0`/`TQ2_0` names for new bit layouts. ⏳ **`Qwen3.8-27B` (16 GB+) and `35B-A3B` are still `401` — 27B is dated Friday 2026-08-14**; at release, header range-read for `nextn_predict_layers` and **append to `t_76235b2a`**, do not card it. Flagship declares `qwen35moe` + MTP head + Mamba-style SSM layers (FULL+MAMBA reuse composition → independent corroboration for **ST-14** `t_13d79be5`). **Before that release the whole name was empty:** all six `Qwen3.8-27B` repos on HF are empty 2-file placeholders** (huginnfork 08-05, neroued 08-06, barozp ×4 today 07:10Z — `.gitattributes` + `README.md`, zero weights). ⛔ **Never pull `Ma7ee7/Qwen3.8_4B_Distilled*`** (41,259 dl on the GGUF) — its own config is `Qwen3ForCausalLM`/`hidden_size 2560` and its card names `base_model: Qwen/Qwen3-4B-Thinking-2507` SFT'd on a 50k dataset of *claimed* `qwen3.8-max-preview` outputs: **lineage naming, not architecture**, and the teacher provenance is one unverifiable hop. Download count is not provenance — the mirror image of the `t_08e96127` veto. **At release, check exactly two things and card neither separately:** does 27B declare `nextn_predict_layers` (free `--spec-type draft-mtp` arm on the `62bf73d` build already on ryan-spark → append to `t_76235b2a`), and is there a **35B-A3B** (the only shape that competes with our banked `qwen3-coder:30b` at 86.7 tok/s — a **dense** 27B carries ~9× the active params/token). Also unmeasured on our own disk: `qwen3.6:35b-a3b` 23.9 GB on the MBP, never benched (SIG-20260812-02)
