Verdict: APPROVE-WITH-NITS

Scope: T1000 branch `ryan/router-templates-testclass` @ 3e6be7d9ca (base `ryan/herald-0.20-cutover` @ 43544a4ed, merge-base confirmed). Executed critic, non-author. Worktree: `.../scratchpad/wt-t1000-router` (detached, read-only + two byte-restored copy-aside mutations). Diff: `scripts/k2_intake_sync.py` +119/-3, `tests/scripts/test_k2_intake_sync.py` +100/-0 (`git diff 43544a4ed 3e6be7d9c --numstat`) — note the brief's "+122" is off by 3 against measured numstat; trivial, flagged under nits.

## 1. [confirmed] Receipt-template clause: kept tip's clause, appended the shape sentence, nothing lost

Isolated unified diff of just the `wiki-staleness`/`split`/`receipt` string body (`diff -u` on the extracted string, both revisions) shows exactly four pure-insertion regions and one same-text reflow — zero base sentences dropped:

- Insert before `ARTIFACT:` (scripts/k2_intake_sync.py:213-221): `SCOPE CEILING, restated because scope-overrun is a recorded close cause...` (#5655/#5656/#5659)
- Insert before `RECEIPT, both halves required:` (scripts/k2_intake_sync.py:223-229): `DUPLICATE-WORK CHECK, before you start...`
- Insert immediately after the base's `"...removed - target gone.\n"` sentence, same line group (scripts/k2_intake_sync.py:263): `SHAPE IS ALSO CHECKED: claim | file:line | quote | verdict is the exact shape 'scripts/quote_fidelity_check.py' enforces... NOT FOUND, the verdict cell must say 'human' or 'unchecked', never 'verified'` — this is the branch's own appended sentence, and it sits directly after the "CARRY EVERY EXISTING ROW FORWARD" / "removed - target gone" clause the brief names, exactly as claimed.
- Old closing (`"If you cannot produce both halves honestly, ... unrecoverable failure here."`) is preserved **word-for-word** as the tail of the new `FINISH PROTOCOL, unconditional` paragraph (scripts/k2_intake_sync.py:280-291) — confirmed by diffing the isolated receipt strings: the only "deletion" in the whole clause is those 3 lines being re-emitted verbatim inside the new paragraph, not lost content.

Rendered through the code's own renderer — not eyeballed. Ran `split["receipt"].format(**fmt)`, the exact call `_create_routed_card` makes at scripts/k2_intake_sync.py:972, via a tmp_path-isolated import (HERMES_HOME/K2_INTAKE_CLONE → `tempfile.TemporaryDirectory()`, mirroring the `mod` fixture at tests/scripts/test_k2_intake_sync.py:19-33) with `fmt = {"page": "docs/knowledge-base/example.md", "sources": "\`scripts/example.py\`", "n_sources": 1}`. Full rendered paragraph (6092 chars, all placeholders resolved, no leftover `{...}` tokens):

> SCOPE — READ THIS FIRST, IT IS NARROWER THAN IT LOOKS. The page is `docs/knowledge-base/example.md`, but you are NOT verifying the whole page. Verify ONLY the claims that rest on the 1 source(s) that changed after the page was last committed: `scripts/example.py`. Every other source the page lists is out of scope — leave those claims alone and do not mention them.
> SCOPE CEILING, restated because scope-overrun is a recorded close cause on this lane (#5655, #5656, #5659 — a fix that spread past its own page and corrupted a sibling): this card touches ONLY `docs/knowledge-base/example.md`'s frontmatter dates and its `## Verification` section. Do not edit, reformat, or 'fix' any other file or page you notice along the way, no matter how tempting. If the real fix needs more than that, STOP and `kanban block` describing what and why — do not widen the diff yourself.
> ARTIFACT: docs/knowledge-base/example.md — the claims deriving from those sources.
> DUPLICATE-WORK CHECK, before you start: five of the lane's close causes were a card verifying a page a human or another card had already handled (stale/superseded — the second-largest recorded cause after vacuous). Check for an OPEN PR already touching `docs/knowledge-base/example.md` before you write anything. If one exists, STOP and `kanban block` naming it — do not race it and do not duplicate the verification.
> RECEIPT, both halves required: [...steps 1-3, verbatim-quote rule, "NOT FOUND"/`human` guidance, "CARRY EVERY EXISTING ROW FORWARD" / "removed - target gone" — unchanged text, see item 2 below...]
>      SHAPE IS ALSO CHECKED: claim | file:line | quote | verdict is the exact shape `scripts/quote_fidelity_check.py` enforces — a row in any other shape fails the gate on format, not content. If the quote cell says `NOT FOUND`, the verdict cell must say `human` or `unchecked`, never `verified`; the same goes for any row with no checkable token and no citation.
>   3. CHANGE NOTHING ELSE. [...unchanged...]
> READ THIS BEFORE YOU DECIDE THE TABLE IS OPTIONAL. [...unchanged...]
> FINISH PROTOCOL, unconditional — re-read your own diff before you call this done. A diff that changes ONLY the two frontmatter dates with no non-empty `## Verification` row is VACUOUS, the modal close cause on this lane (#5376, #5580, #5717, #5688) — refuse it yourself. A `## Verification` section with a header and separator but ZERO data rows is HOLLOW (#5688-shape) — same refusal. If you cannot produce both halves honestly, `kanban block` and say why. Silencing the checker without the evidence is the one unrecoverable failure here.

(Full untruncated render saved at `.../scratchpad/render_receipt.py` output, reproducible by re-running it.) The SHAPE sentence reads coherently in place: it extends the "NOT FOUND ⇒ `human`" rule stated earlier in the same clause to also cover the `verdict` cell's exact allowed values, without contradicting it (`human` remains valid; `unchecked` is added as a second acceptable value, `verified` is newly forbidden for a NOT-FOUND row). No orphaned pronoun, no dangling reference, no duplicate rule.

## 2. [confirmed] Template clauses for the recorded close causes; dedup code byte-identical, hunk list

All three named clauses render present in the same paragraph above and in `tests/scripts/test_k2_intake_sync.py:166-201` (`test_receipt_restates_scope_ceiling`, `test_receipt_has_unconditional_finish_protocol_naming_vacuous_and_hollow`, `test_receipt_has_duplicate_work_check`, `test_receipt_keeps_verbatim_quote_and_not_found_rules`) — all 4 pass (see §5).

**Full hunk list**, `git diff 43544a4ed 3e6be7d9c -- scripts/k2_intake_sync.py` (exactly 4 hunks, all inside the `CHECKERS = [...]` list literal, lines 194-489 tip / 194-371 base):
1. `@@ -210,7 +210,23 @@` — inserts SCOPE CEILING + DUPLICATE-WORK CHECK (pure insertion, 16 new lines)
2. `@@ -244,6 +260,7 @@` — inserts the SHAPE IS ALSO CHECKED sentence (pure insertion, 1 line)
3. `@@ -260,9 +277,16 @@` — replaces the 3-line closing sentence with the 9-line FINISH PROTOCOL paragraph (net +6/-3; the 3 "deleted" lines reappear verbatim inside the new paragraph, see §1)
4. `@@ -368,6 +392,98 @@` — appends the entire new `characterization-tests` dict (93 new lines, 0 deletions)

**Byte-identity proof, not just hunk-absence.** Extracted `scripts/k2_intake_sync.py` at both revisions and diffed by line range:
- Lines 1-193 (module docstring through `CHECKERS = [`, i.e. `_outage_refusal_match`, `_model_relay_reachable`, `is_denied`, all constants): `diff` → empty, byte-identical.
- Line 490 (tip) / 374 (base) `def run_checker(...)` through EOF — i.e. `run_checker`, `_sources_by_page`, `_artifacts`, `_hermes`, `_load_state`, `_save_state`, `_create_parked_card`, `_routed_key`, `_card_status`, `_card_last_run_summary`, `_card_last_event_ts`, `_card_is_stale`, `_card_pr_number`, `_bot_env`, `_mint_bot_token`, `_pr_state`, `_board_has_workdir`, `_create_routed_card`, `_page_has_open_card` (scripts/k2_intake_sync.py:996), `sync` (scripts/k2_intake_sync.py:1037, incl. `state["last_routed_card"]` at :1081 and `counts["skipped_open_page"]`/`counts["skipped_unreadable"]` at :1063/:1379/:1382) — `diff` → empty, byte-identical.
- SHA-256 of the full file matches before/after each of my own restore operations (see §Mutations), confirming the diffing tool wasn't fooled by whitespace-only artifacts.

So the verbatim-quote rule and every named dedup identifier are not merely "no hunk nearby" — they are proven byte-for-byte unchanged, because literally everything from `run_checker` onward is unchanged and the verbatim-quote-rule text sits as unchanged context between hunks 1 and 2 inside the still-touched `CHECKERS` list (confirmed in the isolated receipt-string diff: it appears with no `+`/`-` prefix on either side).

## 3. [confirmed] `characterization-tests` DRAFT checker: parked-only, never ready

Data-level: `{s["key"]: s for s in mod.CHECKERS}["characterization-tests"]` has keys `['argv', 'key', 'line_re', 'split_DRAFT_needs_promotion', 'title']` — **no `"split"` key** (scripts/k2_intake_sync.py:396-479, deliberately named `split_DRAFT_needs_promotion` at :440, per the comment at :432-439).

Code-path level: `sync()`'s splitter gate is `if not spec.get("split"): continue` at scripts/k2_intake_sync.py:1146, reached immediately after the bucket-card (`_create_parked_card`) handling and *before* any routed-card code, including the `_relay_probe_ok_once()`/`_model_relay_reachable()` network probe (scripts/k2_intake_sync.py:1091-1094). Since `spec.get("split")` is `None` for this checker, every run structurally skips the entire routed-mint branch for it — this isn't a naming convention, it's a `continue` the interpreter takes.

Execution level: ran the author's `tests/scripts/test_k2_intake_sync.py::test_characterization_tests_mints_blocked_not_ready` (tests/scripts/test_k2_intake_sync.py:214) — passes. It stubs `_hermes` and `run_checker`, calls `mod.sync(force=True)` with a `characterization-tests` finding (count=2, 2 samples, no `artifacts` key), and asserts `stub.verbs() == ["create", "block"]` with `"needs_input" in stub.calls[1]` — i.e. exactly the `_create_parked_card` sequence at scripts/k2_intake_sync.py:629-646 (`create`, then unconditional `block ... --kind needs_input`), no `promote`, no `unblock`, no `--assignee` (parked cards get none; only `_create_routed_card` at :986 sets `--assignee`). `test_characterization_tests_class_has_no_split_key` (tests/scripts/test_k2_intake_sync.py:203) independently pins the key-absence.

I do not find the brief's literal phrase `"class not promoted: candidate-class-registry row + human-signed record required"` anywhere in the diff (`grep` for "class not promoted" / "candidate-class-registry" / "human-signed" across both files: one hit, the comment at scripts/k2_intake_sync.py:437-438 — "the captain mints the candidate-class-registry row and Ryan signs the promotion record"). Treating it as the brief-writer's gloss on that comment, not a runtime string to assert on — the mechanism it describes (never armed without a human-signed promotion) is what §Mutations #1 below stress-tests directly.

Sanity-checked the checker's own `argv` one-liner (scripts/k2_intake_sync.py:420-430) against a synthetic 3-file scripts/ dir: correctly emits `MISSING_TEST`/`HAS_TEST` per file, matching `line_re = r"^MISSING_TEST\s+\S+"` (:431). Cross-checked its candidate list against the real kevin-real-estate-tools repo on `origin/main`: `check_timer_capacity.py`, `claim_check.py`, `drift_check.py` all exist there with no `test_<name>.py`; `test_lint_timer_recurrence.py` (the deliberately-excluded 4th candidate) does exist there (PR #5734) — the comment's factual claims at scripts/k2_intake_sync.py:73-85 check out. Minor nit: the comment says these three are gated by "`.github/workflows/ci.yml` / `atomic-claim-check.yml`"; `claim_check.py` is in `atomic-claim-check.yml:49` and `check_timer_capacity.py` is in `ci.yml:457`, but `drift_check.py` is actually wired into `.githooks/pre-push:221`, not either named YAML — same "live gate with no test" claim, wrong enforcement mechanism named for 1 of 3. Cosmetic; doesn't change what the checker does.

One coverage-shape observation, not a defect: the pre-existing `test_real_checker_parsers_against_recorded_output` (tests/scripts/test_k2_intake_sync.py:128) pins `line_re`/`summary_re` for the other four checkers against captured live output but was not extended to `characterization-tests`. Its regex is a one-line trivial match on the one-liner's own literal output format, so the gap is low-risk, but it means the "never ready" guarantee is what's tested here, not the parser's fidelity against real `check_timer_capacity.py`/`claim_check.py`/`drift_check.py` output.

## 4. [confirmed] Zero functions touched — routing/throttle/slot logic untouched

Every one of the 4 hunks lands inside the `CHECKERS` module-level list literal (scripts/k2_intake_sync.py:194-489), which is data, not code. Per §2's byte-identity proof, **every function in the file is unchanged**, including all routing/throttle/slot logic: `sync` (throttle at :1040, `MAX_NEW_CARDS_PER_RUN`/`MAX_NEW_ROUTED_PER_RUN`/`MAX_ROUTED_IN_FLIGHT` gates), `_create_routed_card`, `_card_is_stale`, `_routed_key`, `_page_has_open_card`. **List of changed functions: none.** The diff is confined to (a) editing one string literal (`wiki-staleness`'s `split.receipt`) and (b) appending one new dict entry (`characterization-tests`) to the list — confirmed structurally by the diff's own hunk-header context (`CHECKERS = [` on all 4 headers, never a `def` line) and confirmed byte-for-byte by the extracted-range diffs in §2.

## 5. [confirmed-partial / one claim refuted] Suite

Ran `/Users/ryan/Documents/T1000/venv/bin/python -m pytest -q -p no:cacheprovider tests/scripts/test_k2_intake_sync.py tests/scripts/test_k2_intake_sync_dedupe.py` — **17 passed** (both files, tip). All 6 new tests pass individually and together (`-k "receipt or characterization"` → 6 passed).

The brief's "339 passed + 2 inherited failures" maps to the full `tests/scripts/` dir (341 tests collected — 339+2 — matching exactly; the two named files alone total 17, not 339, so I ran `tests/scripts/` as the intended scope):
- **Tip (3e6be7d9c), `tests/scripts/`: 340 passed, 1 failed**, in 9.16-9.98s across 3 repeated runs (identical each time).
- **Base (43544a4ed), `tests/scripts/`, second detached worktree** (`git -C /Users/ryan/Documents/T1000 worktree add --detach .../wt-t1000-router-base 43544a4ed`, per the brief — no stash, no branch switch on the primary worktree): **334 passed, 1 failed** (335 total = 341 - 6 new tests, consistent).
- The one failure that DOES reproduce, both revisions: `test_footgun_subprocess_encoding.py::TestFullRepoScan::test_new_rule_find_only_known_violations`, byte-identical error both times — `"New footgun rule found UNEXPECTED matches in files not covered by PR #60741: ['scripts/t1000_distillery_review_apply.py']"`. Confirmed **inherited**, unrelated to this diff (this branch never touches that file).
- **[refuted]** `test_playbook_sweep.py::test_state_dedupe_prevents_recomment` — the brief's second named "inherited failure" — did **not** fail for me anywhere: not in either full-suite run (grep for it in both full-suite logs: zero hits), and not in 8 additional isolated runs (5× at tip, 3× at base, `pytest tests/scripts/test_playbook_sweep.py::test_state_dedupe_prevents_recomment` — 8/8 passed). This branch's diff never touches `test_playbook_sweep.py` or its subject module, so this can't be something the branch fixed or broke either way — it reads as a stale/flaky claim in the author's report rather than a defect here. Flagged, not blocking.

## Mutations (copy-aside → edit → targeted tests → restore, cmp)

Backup taken before either mutation: `cp scripts/k2_intake_sync.py .../k2_intake_sync.py.orig-backup`, SHA-256 `9634a84b1461263de9613679c7e79ba03590a5dd0147dac0342749fa2f0e339e`.

**Mutation 1 — prove the DRAFT guard is real, not tautological.** Edited scripts/k2_intake_sync.py:440 in place, renaming `"split_DRAFT_needs_promotion"` → `"split"` (exactly the "arming act" the comment at :436 warns against). Result: `test_characterization_tests_class_has_no_split_key` **fails** (`AssertionError: assert 'split' not in {...}`); `test_characterization_tests_mints_blocked_not_ready` still passes, because its stubbed finding carries no `"artifacts"` key so the now-reachable splitter loop (scripts/k2_intake_sync.py:1148 `live = finding.get("artifacts") or []`) iterates zero items — i.e. the hermes-sequence test alone would **not** have caught this regression; the key-name guard test is the one carrying that weight. Went one step further with an ad hoc driver (`.../scratchpad/prove_arming_risk.py`, tmp_path-isolated, `_hermes` stubbed, `_board_has_workdir` stubbed true) supplying a finding **with** `"artifacts": ["scripts/claim_check.py"]`: with the mutation applied, `sync()` did reach the routed-splitter branch and called `_relay_probe_ok_once()` → `_model_relay_reachable()` (scripts/k2_intake_sync.py:132-145, a real `socket.create_connection` attempt to the tailnet model host, 3s timeout) before deferring the card under "outage hold" (the sandbox has no tailnet route, so the probe failed and nothing minted) — proving concretely that arming this key would route real findings into the same live-network-touching path wiki-staleness uses. Disclosed for transparency: this was one bounded, timeout-limited outbound TCP attempt to a private tailnet address made only under my own deliberately-mutated file with `_hermes` fully stubbed — no ssh, no real `hermes` call, no live state file touched (STATE_PATH stayed under tmp_path throughout). Restored: `cp .../k2_intake_sync.py.orig-backup scripts/k2_intake_sync.py`; `cmp` → byte-identical; SHA-256 matches original; `git status --porcelain` → clean.

**Mutation 2 — prove the new receipt-clause tests aren't vacuous.** Edited scripts/k2_intake_sync.py, deleting the 9-line SCOPE CEILING insertion (:213-221) from the wiki-staleness receipt. Ran the 6 new tests: **`test_receipt_restates_scope_ceiling` fails** (`AssertionError: assert 'SCOPE CEILING' in '...'`), all other 5 still pass — precisely targeted, not coupled to unrelated content. Restored: `cmp` → byte-identical; SHA-256 matches original; `git status --porcelain` → clean. Re-ran full 17-test pair post-restore: 17 passed.

Second worktree removed after use: `git -C /Users/ryan/Documents/T1000 worktree remove --force .../wt-t1000-router-base` (confirmed gone from `git worktree list`). Primary worktree confirmed clean throughout and at the end (`git status` → "nothing to commit, working tree clean"; no ssh, no push, no `git stash`, no `sync()` call outside tmp_path-isolated fixtures/scripts).

## Residual risk

- The suite-baseline discrepancy in §5 (only 1 of the claimed 2 "inherited failures" reproduces) is worth a quick follow-up with whoever generated the "339 passed + 2 failures" report — either their run was stale/dirty or `test_state_dedupe_prevents_recomment` is order/environment-flaky in a way that didn't trigger for me in 11 total runs. Doesn't implicate this branch's diff.
- The `characterization-tests` checker's own parser (`line_re`/one-liner) has no regression pin against recorded output the way the other four checkers do (§3) — low risk given the trivial regex and the DRAFT/parked-only status, but worth a follow-up test if/when this class is ever promoted.
- Promotion-time risk (not this PR's problem, but worth naming since I stress-tested it): the only thing standing between this checker and live routed-card minting — including the real model-relay network probe — is the literal string `"split"` as a dict key, guarded today by a comment and one test asserting its absence (tests/scripts/test_k2_intake_sync.py:203). That's exactly as strong as the base's own pattern for parts-catalog's paused entry (scripts/k2_intake_sync.py:339), so it's consistent with existing practice, not a new weakness — but it means the *only* enforcement is code review + this one test, same as always.
- Two brief-accuracy nits, neither a code defect: diff-stat "+122" vs measured "+119" insertions on scripts/k2_intake_sync.py; and the drift_check.py enforcement-mechanism naming in the new code's own comment (scripts/k2_intake_sync.py:73-85) says `.github/workflows/ci.yml` / `atomic-claim-check.yml` where the real gate is `.githooks/pre-push:221`.
